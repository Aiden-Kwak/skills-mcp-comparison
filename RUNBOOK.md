# 데모 진행 순서표 (RUNBOOK)

**시연 시나리오**: 2종
1. **A — Skill만 활성** (MCP 없음)
2. **B — Skill + MCP 둘 다 활성**

**소요 시간**: 약 7~10분 (셋업 1분 + A 3분 + 전환 30초 + B 4분 + 마무리 1분)

---

## 0. 사전 점검 (녹화 시작 직전, 화면 밖에서)

```bash
cd /Users/aiden-kwak/Desktop/Personal/mcpvsskills

# DB가 시드되어 있는지
ls -lh data/bugs.db

# MCP 서버가 동작하는지 (10초 안에 끝남)
printf '%s\n' \
  '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}' \
  '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"get_stats","arguments":{"since_days":7}}}' \
  | python3 mcp_server/bug_triage_server.py

# total_in_window: 23 이 보이면 정상
```

녹화 직전 시작 상태를 **A(Skill만)** 로 맞춰둠:

```bash
./demo.sh skill-only
```

Claude Code는 아직 실행 안 한 상태로 둔다.

---

## 1. 인트로 컷 (30초)

말할 것:
> "사내 버그 트래커에 100건 가까운 버그가 쌓여있습니다. 같은 일을 AI에게 시킬 건데, **두 가지 환경**에서 결과가 어떻게 다른지 보겠습니다."

화면에 보일 것: 빈 터미널 + 프로젝트 디렉토리 (`ls` 한 번 보여주기)

```bash
ls
# data/  mcp_server/  skill/  scenarios/  expected_outputs/
# REPORT.md  README.md  RUNBOOK.md  DATASET.md  demo.sh  .mcp.json.off  .claude/
```

(`.mcp.json.off` 가 보이면 자연스럽게 "지금 MCP는 꺼져 있는 상태입니다" 멘트)

---

## 1.5. 데이터셋 인지 컷 (1분, ⭐ 중요)

> **"이 버그가 진짜인가요?" 라는 의심을 미리 차단하는 단계.**
> 데모 신뢰도를 위해 반드시 먼저 진행.

### 1.5-1. 데이터셋 요약 출력

```bash
./demo.sh dataset
```

화면에 다음과 같이 출력됨 (배너 + 분포 + 의도된 트랩 8건):

```
╔══════════════════════════════════════════════════════════════════╗
║   📊  데모용 시드 데이터셋 (Synthetic Dataset for Demo)              ║
║   ⚠️  실제 운영 데이터 아님 — SQLite 파일에 직접 입력한 가상 버그       ║
╚══════════════════════════════════════════════════════════════════╝

  ─── 의도적으로 심어둔 트랩 ───
  #3 low    → 🚨 critical  비밀번호 reset 만료 후 작동       인증 우회
  #4 medium → 🚨 critical  관리자 페이지 워크스페이스 누출    데이터 격리 위반
  #5 medium → 🚨 critical  CSV export 다른 사용자 행 섞임   데이터 격리 위반
  ...
```

### 1.5-2. 시연 멘트 (핵심)

> "먼저 명확히 말씀드릴 것이 있습니다. **지금부터 보시는 버그들은 실제 운영 데이터가 아닙니다.** 이 자리에서 직접 만든 SQLite 가상 데이터입니다.
>
> 다만 이 안에는 의도적으로 **실제 SaaS 회사에서 자주 놓치는 패턴**을 심어뒀습니다 — 보세요, 신고자가 *low*나 *medium*으로 잡았지만 실제로는 *critical*인 보안 이슈 3개, 한·영·내부에서 따로 신고된 중복 3건, 사실 기능 요청인데 버그로 분류된 2건.
>
> AI가 이것들을 잡아내는지를 보는 게 오늘 데모의 핵심입니다."

### 1.5-3. (선택) 슬라이드/문서 화면 공유

`DATASET.md` 를 화면 절반에 띄워놓고 시작하면 청중이 시연 중 언제든 다시 볼 수 있어 좋습니다.

```bash
open DATASET.md   # macOS
```

---

## 2. 시나리오 A — Skill만 활성

### 2-1. 상태 확인

```bash
./demo.sh status
```

**기대 출력**:
```
  bug-triage  Skill : ON
  bug-triage  MCP   : OFF
```

말할 것:
> "트리아지를 *어떻게 해야 하는지* 알려주는 Skill만 켜져 있습니다. 데이터 접근 도구는 없습니다."

### 2-2. Claude Code 실행

```bash
claude
```

(이미 켜져 있다면 한 번 종료 후 재실행 — `.claude/skills/`를 새로 읽기 위해)

### 2-3. 프롬프트 입력 (복붙용)

```
지난 7일간 들어온 버그를 트리아지해서 가장 시급한 5개와 처리 권고안을 작성해줘.
```

### 2-4. 기대 응답 (요지)

> bug-triage Skill을 가지고 있어 트리아지 절차는 안내 가능합니다:
> - Step 1: 카테고리 분리
> - Step 2: 숨은 크리티컬 탐지
> - Step 3: 우선순위 점수
> - Step 4: 권고 액션
>
> 다만 실제 버그 데이터에 접근할 수 있는 도구가 없어 적용은 불가능합니다. 버그 목록을 제공해 주시거나 MCP 서버를 연결해 주세요.

### 2-5. 시연 멘트

> "보시는 것처럼 AI는 *방법론*은 알고 있습니다. 하지만 실제 데이터가 없으니 적용을 못 합니다. 결국 사용자가 데이터를 직접 복사해서 가져와야 합니다."

(이 응답을 화면에 잠깐 멈춰서 캡처)

### 2-6. Claude Code 종료

`/exit` 또는 Ctrl+C 두 번

---

## 3. 전환 컷 (30초)

말할 것:
> "이번에는 똑같은 환경에 **MCP만 추가**해 보겠습니다. AI에게 사내 버그 DB에 접근할 수 있는 도구를 주는 겁니다."

화면에 보일 것: 다음 명령어

```bash
./demo.sh both
```

**기대 출력**:
```
  bug-triage  Skill : ON
  bug-triage  MCP   : ON
```

(`.mcp.json` 파일이 다시 활성화된 것을 `ls`로 한 번 보여주면 좋음)

```bash
ls .mcp.json
# .mcp.json   ← 이제 .off가 떨어짐
```

---

## 4. 시나리오 B — Skill + MCP

### 4-1. Claude Code 재실행

```bash
claude
```

세션 시작 시 MCP 연결 확인 (`/mcp` 명령어로 연결 상태 확인 가능):

```
/mcp
```

**기대**: `bug-triage` 서버가 connected 상태로 표시됨.

### 4-2. 프롬프트 입력 (시나리오 A와 완전히 동일)

```
지난 7일간 들어온 버그를 트리아지해서 가장 시급한 5개와 처리 권고안을 작성해줘.
```

### 4-3. 기대 응답 흐름

Claude가 다음 순서로 동작:

1. **MCP 도구 호출 표시**:
   - `get_stats(since_days=7)` → 23건 통계
   - `list_bugs(since_days=7)` → 23건 목록
   - `get_bug(1)`, `get_bug(3)`, `get_bug(4)`, `get_bug(5)` → 의심 항목 상세
   - `search_bugs("중복")`, `search_bugs("duplicate")` → 클러스터 탐지

2. **Skill 적용된 트리아지 결과**:

   ```
   ## 트리아지 요약
   - 분석 대상: 23건 (최근 7일)
   - 진짜 버그: 18 / 중복: 3(1 클러스터) / 기능요청: 2
   - 🚨 숨은 크리티컬 발견: 3건 (재평가)

   ## 🔥 즉시 처리 Top 5
   1. [#1] 결제 500 에러 — score 67.5, 즉시 에스컬레이션
   2. 🚨 [#5] CSV export (medium→CRITICAL) — 데이터 격리 위반
   3. 🚨 [#4] 관리자 페이지 (medium→CRITICAL) — 워크스페이스 누출
   4. 🚨 [#3] 비밀번호 reset (low→CRITICAL) — 인증 우회
   5. [#2] DB 30초 — 진행 추적

   ## 📦 중복 클러스터: #6/#7/#8 → #6으로 통합
   ## 💡 기능 요청: #9 다크모드, #10 차트export → 백로그
   ```

### 4-4. 시연 멘트 (이 부분이 핵심)

응답이 나오는 동안:
> "보세요. AI가 직접 DB를 조회합니다. `get_stats`, `list_bugs`, 그리고 의심되는 버그는 `get_bug`로 상세까지 가져옵니다."

응답이 완성되면 **Top 2~4 항목**을 줌인해서:
> "여기 중요한 게 있습니다. 신고자가 *medium*으로 잡았던 이 CSV export 버그, 그리고 *low*로 잡았던 비밀번호 reset 버그가 모두 **critical로 재평가**됐습니다. Skill에 정의된 '데이터 격리 위반', '인증 우회' 패턴을 description에서 찾아낸 겁니다."

중복 클러스터 부분도 짚으며:
> "그리고 한국어·영어·내부에서 따로따로 신고된 검색 중복 3건도 하나로 묶었습니다. 단순 SQL 쿼리로는 못 잡는 부분이죠."

---

## 5. 마무리 컷 (1분)

A와 B의 응답을 화면 좌우로 띄워놓고:

말할 것:
> "같은 프롬프트인데 결과가 완전히 다릅니다.
>
> **A는 *방법*만 있고 *데이터*가 없어서 못 했고**,
>
> **B는 *데이터*를 가져와서 *방법*을 적용해 — 사람이 바로 액션할 수 있는 결과를 줬습니다.**
>
> MCP는 AI에게 사내 시스템에 대한 *능력*을 주고, Skill은 *판단 기준*을 줍니다. 둘 다 있어야 의사결정 가능한 결과가 나옵니다."

(추가 옵션) 시나리오 3(MCP만)을 미리 녹화해뒀다면 마지막에 보여주며:
> "참고로 MCP만 있고 Skill이 없으면 어떻게 되는지도 따로 찍어뒀습니다. 데이터는 가져오는데 그냥 severity 필드 그대로 정렬해서, 방금 본 숨은 critical 3건을 다 놓칩니다."

---

## 6. 명령어 치트시트 (한 페이지 요약)

| 시점 | 명령어 |
|-----|--------|
| 사전 점검 | `cd /Users/aiden-kwak/Desktop/Personal/mcpvsskills` |
| 시작 상태 | `./demo.sh skill-only` |
| **데이터셋 인지** | `./demo.sh dataset`  ← 청중 신뢰도용 |
| Claude 실행 | `claude` |
| 프롬프트 (A) | `지난 7일간 들어온 버그를 트리아지해서 가장 시급한 5개와 처리 권고안을 작성해줘.` |
| Claude 종료 | `/exit` |
| 전환 | `./demo.sh both` |
| Claude 재실행 | `claude` |
| MCP 연결 확인 | `/mcp` |
| 프롬프트 (B) | (A와 동일, 복붙) |
| 종료 후 정리 | `./demo.sh off` |

---

## 7. 트러블슈팅

| 증상 | 원인 | 해결 |
|------|------|------|
| MCP 도구 호출 안 함 | 세션 재시작 안 함 | `/exit` 후 `claude` 재실행 |
| `/mcp` 에 bug-triage 안 보임 | `.mcp.json` 비활성 상태 | `./demo.sh both` 다시 실행 |
| Skill이 적용 안 됨 (그냥 일반 응답) | Skill 파일 못 찾음 | `ls .claude/skills/bug-triage/SKILL.md` 확인 |
| Python3 not found | python 경로 차이 | `which python3` 확인 후 `.mcp.json` command 수정 |
| DB 파일 없음 | 시드 안 됨 | `python3 data/seed_db.py` |
| 시연 중 응답 다르게 나옴 | 모델이 다른 도구 호출 순서 사용 | 정상. 핵심은 **Top 5 구성**과 **재평가 배지** |

---

## 8. 응답 길이 컨트롤 팁

Claude의 응답이 너무 길어 화면 안에 안 들어오면, 두 시나리오 모두에 다음 한 줄을 추가:

```
지난 7일간 들어온 버그를 트리아지해서 가장 시급한 5개와 처리 권고안을 작성해줘. 핵심만 표 형태로 짧게.
```

이렇게 하면 시나리오 A는 "데이터 없어서 표 작성 불가"가 더 명확하게 드러나고, B는 표 한 장으로 깔끔하게 비교 가능.
